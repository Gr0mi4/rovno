# Android screenshots

Release workflow `emulator-api35` uploads `android-preview.png` from a signed release build on API 35.

After a successful tag build, download the `emulator-smoke-api35` artifact and copy screenshots here for README updates (dark/light, landscape, offline states as needed).
